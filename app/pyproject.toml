[project]
name = "watcher"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "ollama==0.5.2",
    "pillow>=11.3.0",
    "psutil>=7.0.0",
    "pyyaml>=6.0.2",
    "requests>=2.32.4",
]
